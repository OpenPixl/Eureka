<form method="post" action="{{ path('op_appli_bookroom_delete', {'id': bookroom.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bookroom.id) }}">
    <button class="btn">Delete</button>
</form>
