{{ form_start(form) }}
<div class="mb-4">
    {# STRING : Ligne du nom d la matière #}
    <div class="row mb-1 mt-1 g-1">
        <div class="col-12">
            {{ form_widget(form.name, {'attr': {'class': 'form-control form-control-sm', 'placeholder': 'Nom de la salle'}}) }}
        </div>
    </div>
    {# STRING : input logo #}
    <div class="row mb-1 mt-1 g-1">
        <div class="col-4 d-flex align-items-center">
            {{ form_label(form.maxPlace) }}
        </div>
        <div class="col-8">
            {{ form_widget(form.maxPlace, {'attr':{'class':'form-control form-control-sm'}}) }}
        </div>
    </div>
    {{ form_widget(form) }}
    <div class="row g-1 mt-4 mb-4">
        <div class="col-3">
            <a class="btn btn-sm btn-primary w-100" href="{{ path('op_appli_room_index') }}">Retour</a>
        </div>
        <div class="col-9">
            <button class="btn btn-sm btn-primary w-100">{{ button_label|default('Ajouter') }}</button>
        </div>
    </div>
</div>
{{ form_end(form) }}
