<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <script src="https://kit.fontawesome.com/1fb9149f4c.js" crossorigin="anonymous"></script>

        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
            <script src="https://npmcdn.com/flatpickr/dist/l10n/fr.js"></script>
        {% endblock %}
    </head>
    <body>
        <header class="d-lg-none">
            {% block header %}
                {{ include('include/_navbar.html.twig') }}
            {% endblock %}
        </header>
        <main class="d-lg-none">
            <div class="container-sm">
                {% block main %}
                <div class="row">
                    <div class="col-12 d-flex justify-content-center mt-5 mt-5">
                        <img src="{{ asset('images/svg/LogoIntro.svg') }}" class="img-fluid">
                    </div>
                    <div class="col-12 d-flex justify-content-center mt-5 mt-5">
                        <img src="{{ asset('images/svg/TitleEuréka.svg') }}" class="img-fluid">
                    </div>
                </div>
                {% endblock %}
            </div>
            {% block modal %}
            {% endblock %}
        </main>
        <footer class="d-lg-none">
            <div class="container-sm">
            {% block footer %}
                <section id="blockCommand" class="mb-3">
                    <div class="row">
                        <div class="col-6 d-flex justify-content-center">
                            <a href="#"><i class="fa-thin fa-user fa-2xl"></i></a>
                        </div>
                        <div class="col-6 d-flex justify-content-center">
                            <a href="{{ path('op_admin_dashboard_super') }}"><i class="fa-thin fa-gear fa-2xl"></i></a>
                        </div>
                    </div>
                </section>
                <section>
                    <p class="text-center">OpenPixl - KhaliKréa</p>
                </section>
            {% endblock %}
            </div>

        </footer>
    </body>
</html>
